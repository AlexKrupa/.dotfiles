# Repo-relative paths symlinked from the main checkout into every new worktree.
# A repo can add more in .herdr-links at its main checkout root. The lists merge.
local.properties
CLAUDE.local.md
.claude/settings.local.json
