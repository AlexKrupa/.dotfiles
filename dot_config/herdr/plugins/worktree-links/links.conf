# Repo-relative paths symlinked from the main checkout into every new worktree.
# A repo adds more in .herdr-links at its main checkout root. The lists merge.
# A trailing slash (.idea/) links the entries inside the directory, one by one.
local.properties
CLAUDE.local.md
.claude/
.idea/
