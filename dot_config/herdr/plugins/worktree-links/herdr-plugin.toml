id = "local.worktree-links"
name = "Worktree links"
version = "0.1.0"
min_herdr_version = "0.7.0"
description = "Symlink shared local files from the main checkout into new worktrees"
platforms = ["macos"]

[[events]]
on = "worktree.created"
command = ["./link.sh"]
